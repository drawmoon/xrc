package xray

import "github.com/drawmoon/xrc/pkg/proto"

// newTrojanConfig generates an Xray configuration for a Trojan link.
func newTrojanConfig(t *proto.Trojan) (*Config, error) {
	return &Config{}, nil
}
